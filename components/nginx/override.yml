version: '3.9'

services:
  nginx:
    ports:
      - "${NGINX_HTTP_PORT:-80}:80"
      - "${NGINX_HTTPS_PORT:-443}:443"
    volumes:
      - ./components/nginx/conf.d:/etc/nginx/conf.d:ro
      - ./components/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - nginx_logs:/data/log/nginx