version: '3.9'

services:
  mongo:
    ports:
      - "${MONGO_PORT:-27017}:27017"
    volumes:
      - ./components/mongo/mongod.conf:/etc/mongod.conf:ro
      - /data/mongo:/data/db
      - /data/logs/mongo:/var/log/mongo