version: '3.9'

services:
  mysql:
    ports:
      - "${MYSQL_PORT:-3306}:3306"
    volumes:
      - ./components/mysql/my.cnf:/etc/mysql/conf.d/my.cnf:ro
      - ./components/mysql/init:/docker-entrypoint-initdb.d:ro
      - /data/mysql:/var/lib/mysql
      - /data/logs/mysql:/var/log/mysql